# Environment variables declared in this file are NOT automatically loaded by Prisma.
# Please add `import "dotenv/config";` to your `prisma.config.ts` file, or use the Prisma CLI with Bun
# to load environment variables from .env files: https://pris.ly/prisma-config-env-vars.

# Prisma supports the native connection string format for PostgreSQL, MySQL, SQLite, SQL Server, MongoDB and CockroachDB.
# See the documentation for all the connection string options: https://pris.ly/d/connection-strings

DATABASE_URL="postgresql://deefoodie:deefoodie_dev@localhost:5435/deefoodie?schema=public"

# Photo storage — local (default) or S3-compatible (AWS S3, Cloudflare R2, etc.)
STORAGE_DRIVER=local
UPLOAD_DIR=./uploads

# S3 mode (set STORAGE_DRIVER=s3)
# S3_BUCKET=deefoodie-photos
# S3_REGION=auto
# S3_ACCESS_KEY_ID=
# S3_SECRET_ACCESS_KEY=
# S3_ENDPOINT=https://<account>.r2.cloudflarestorage.com
# S3_PUBLIC_BASE_URL=https://photos.yourdomain.com